export default function App() {
  return (
    <div className="min-h-screen bg-gray-50">
      <nav className="bg-white border-b p-3 flex gap-3 shadow-sm">
        <span className="font-semibold text-gray-700">ReStockOffice</span>
      </nav>
      <main className="p-6">
        {/* Feature-Views folgen in den Feature-Branches */}
      </main>
    </div>
  );
}
